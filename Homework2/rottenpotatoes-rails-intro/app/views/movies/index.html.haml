-#  This file is app/views/movies/index.html.haml
-# Code for this file based on this video: https://www.youtube.com/watch?v=xyi4YnX7LUQ
%h1 All Movies

%table#movies
  %thead
    %tr
      -# Set the class as hilite if we use this column to sort the entries
      %th{:class=>("hilite" if @column_sort_by == "title")}= link_to 'Movie Title', movies_path(:sort_by=>'title'), :id=>"title_header"
      %th Rating
      %th{:class=>("hilite" if @column_sort_by == "release_date")}= link_to 'Release Date', movies_path(:sort_by=>'release_date'), :id=>"release_date_header"
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= link_to movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Add new movie', new_movie_path
